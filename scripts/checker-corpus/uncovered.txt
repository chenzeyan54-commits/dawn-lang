# `cerr` call sites in the checker's modules that no corpus case reaches.
# Regenerate with `./scripts/checker-corpus/run.sh --record`; the reason column
# is kept across regenerations, so write it once.
#
# A ratchet in both directions (see coverage.py): a new unreached site fails
# the build, and so does a listed site that became reachable.
#
# 291 sites, 286 reached, 5 here.
#
#   <message expression>	<why no case reaches it>
  "pattern analysis exceeded its complexity budget"	TODO: reason
  "unsafe_pure cannot mask the effect variable !" ++ eff_show(p) ++ " — it only vouches that a concrete io effect is absent"	behind the `not cx.is_std_module` early return in check_unsafe_pure: user code is refused before the body is inspected. Only a std module reaches this branch.
  "redundant unsafe_pure: the block is already pure"	same early return as the line above -- std-only.
  "the derived identity of " ++ minted_show(decl) ++ " collides with " ++ minted_show(other)	needs two declarations whose 47-bit digests agree, which no source file can be written to produce. `check/cx.dawn`'s "two declarations that derive one id" test plants the table state instead, and `incremental-semantics-contract/allocation.py`'s intern-collision-ignored mutant is the control.
  "`use java` is not available in this compiler"	fires when `cx.jsig` is the refusing default, which is the native driver. `dawn check` runs on the JVM with `jsig_real()`, so no case run by this corpus can reach it.
